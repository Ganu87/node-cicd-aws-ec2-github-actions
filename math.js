// Add functions for basic arithmetic operations
const add = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    return a - b;       
}

const multiply = (a, b) => {
    return a * b;
}   
// add div function

module.exports={
    add: add,
    subtract: subtract,
    multiply: multiply
}
