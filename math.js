// Add functions for basic arithmetic operations
const add = (a, b) => {
    return a + b;
}

const subtract = (a, b) => {
    return a - b;       
}

const multiply = (a, b) => {
    return a * b;
}   

/// created new dev1.1 branch

// add func 111
module.exports={
    add: add,
    subtract: subtract,
    multiply: multiply
}
